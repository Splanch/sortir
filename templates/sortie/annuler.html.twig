{% extends 'layout.html.twig' %}

{% block main %}

    {{ info.id }}
    {{ info.nom }}
    {{ form_start(annulation) }}
    {{ form_row(annulation.motif) }}
    {{ form_end(annulation) }}



{% endblock %}